# Wordoria GitHub Pages 发布说明

这份项目已经加入 GitHub Pages 静态发布配置。

## 你要做的鼠标步骤

1. 登录 GitHub。
2. 右上角点 `+`。
3. 点 `New repository`。
4. 仓库名建议填 `wordoria`。
5. 选择 `Public`。
6. 不要勾选 `Add a README file`。
7. 点 `Create repository`。
8. 把当前项目上传到这个仓库。
9. 打开仓库页面，点 `Settings`。
10. 左侧点 `Pages`。
11. `Source` 选择 `GitHub Actions`。
12. 回到仓库首页，点 `Actions`，等待 `Deploy GitHub Pages` 变成绿色。

成功后，别人可以打开：

```text
https://你的GitHub用户名.github.io/wordoria/
```

如果你的仓库名不是 `wordoria`，链接最后一段要换成你的仓库名。

## 注意

- ChatGPT Sites 线上版仍然保留，不受影响。
- GitHub Pages 版是静态网页，适合分享给别人直接访问。
- 学习记录仍保存在访问者自己的浏览器里，不会同步到服务器。
